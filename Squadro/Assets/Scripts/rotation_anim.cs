using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class rotation_anim : MonoBehaviour
{
    public GameObject PionRotate;
    // Start is called before the first frame update
    void Start()
    {
        
    }
    
    // Update is called once per frame
    void Update()
    {
          //PionRotate;.GetComponent<Animator>().Play("rotation_animation");
    }
    
}
